node_modules/
dist/
.env
.env.local
contracts/managed/
.DS_Store
*.log
spikes/dr1_native_night/node_modules/
spikes/dr1_native_night/managed/
~/.lumpfun/
test-wallets/

# Aiken build output — regenerated by `aiken build`
contracts/cardano/build/
contracts/cardano/aiken-compile.lock

# Playwright MCP debug artifacts
.playwright-mcp/

# Test session state — preprod only; do not commit on mainnet.
# Comment this out if you intentionally want to ship a registry snapshot.
cardano-registry.json

# tsbuildinfo
*.tsbuildinfo
.vercel
.env*.local
